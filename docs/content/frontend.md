# Frontend

The frontend is a Vue 3 + Vite application that uses `picocrank` and `femtocrank` for components and styling. Vue Router handles client-side routing.

## Development

```bash
make -C frontend dev
```

## Production build

```bash
make -C frontend build
```
